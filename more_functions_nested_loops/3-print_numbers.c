#include "main.h"
#include <stdio.h>
/**
 * main - Prints the alphabet except e and q
 *
 * Return: Always (Success)
 */

void print_most_numbers(void)
	char a;

	for (a = '0'; a <= '9' ; al++)
		if (a != '2' && a != '4')
	{
			putchar(a);
	}
	putchar('\n');
	return (0);
}
