#include "main.h"
/**
 * _strlen - returns length of string
 * @s: string
 * Return: i
 */
int _strlen(char *s)
int i;

{
	for (i = 0; str[i] != '\0'; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
