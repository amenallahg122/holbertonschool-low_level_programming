#include "main.h"

/**
 * _islower -  prints _putchar, followed by a new line
 * Return: Always 0 (Success)
 * @c : integer
 */

int isalpha(int c)
{

	if c >= 65  && c <= 122
	{
		return (1);
	}
	else
		return (0);
}
