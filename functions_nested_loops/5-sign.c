#include "main.h"

/**
 * print_sign -  prints sign of number
 * Return: Always (Success)
 */

int _print_sign(int n)
{
	if (0 > n)
	{
		_putchar(+) return (1);
		return (1);
	}
	else if (n == 0)
	{
		_putchar(0) return (0);
	}
	else
	{
		_putchar(-) return (-1);
	}
}
