#include "main.h"

/**
 * int _abs - computes the absolute value of an int
 * @n: number
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		m = (n * -1)
		return (m);
	}
}
