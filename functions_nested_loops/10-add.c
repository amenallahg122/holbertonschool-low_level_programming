#include "main.h"
/**
 * add - adds two integers together
 * A: int
 * B: int
 * sum: int
 * Return: result of the addition
 */

int add(int A, int B)
{
    int sum = 0;
    sum = A + B;
    return (sum);
}
